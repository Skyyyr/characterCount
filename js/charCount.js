/**
 * @param {string} word A given string that contains lower case letters, and may have repeat occurances
 * @returns list[letter : amountOfOccurances] in order from highest occurances to lowest
 */
exports.charCount = function(word) {
    output_listing = {}
    const wordArray = word.split('');
    console.log(word, wordArray)
    return output_listing
};